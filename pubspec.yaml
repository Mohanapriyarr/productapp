name: product_app
version: 1.0.0+1
publish_to: none
description: A new Flutter project.
environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  cupertino_icons: ^1.0.2
  fluttertoast: ^8.0.9
  # for api
  http: ^0.13.5
  # for logging and color print
  logging: ^1.1.1
  palestine_console: ^0.0.10
  to_curl: ^2.0.0-nullsafety.0 #to get http curl
  # for loading
  loading_animation_widget: ^1.2.0+4
  uuid: ^4.3.3
  get: 4.6.6
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: 3.0.1
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/images/
